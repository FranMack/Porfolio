import "./globals.css";

export const metadata = {
  title: "Test - Francisco Mackinnon",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>
        {children}
      </body>
    </html>
  );
}